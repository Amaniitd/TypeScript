
class Product{
    name: string;
    price: number;
    id: number;
    constructor(name: string, price: number) {
        this.name = name;
        this.price = price;
        this.id = 0;
    }
}

// Suppose some objects have already has been created
//and we want to add some new property to them
// Modify just by using the class??



// -> decorator 
